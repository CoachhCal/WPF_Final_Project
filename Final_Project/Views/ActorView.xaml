<UserControl x:Class="Final_Project.Views.ActorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Final_Project.Views"
             xmlns:vm="clr-namespace:Final_Project.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="LightBlue">
    <StackPanel Margin="10">

        <TextBlock Text="Actor Details" FontSize="24" FontWeight="Bold" Foreground="Pink" Margin="0,0,0,20"/>

        <TextBlock Text="Name:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding PrimaryName}" Margin="0,0,0,10"/>

        <TextBlock Text="Profession:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding PrimaryProfession}" Margin="0,0,0,10"/>

        <TextBlock Text="Birth Year:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding BirthYear}" Margin="0,0,0,10"/>

        <TextBlock Text="Death Year:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding DeathYear}" Margin="0,0,0,10"/>
    </StackPanel>
</UserControl>
