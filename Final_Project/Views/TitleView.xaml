<UserControl x:Class="Final_Project.Views.TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">

        <TextBlock Text="Title Details" FontSize="24" FontWeight="Bold" Foreground="Blue" Margin="0,0,0,20"/>

        <TextBlock Text="Title Type:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding TitleType}" Margin="0,0,0,10"/>


        <TextBlock Text="Original Title:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding OriginalTitle}" Margin="0,0,0,10"/>

        <TextBlock Text="Is Adult:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding IsAdult}" Margin="0,0,0,10"/>

        <TextBlock Text="Start Year:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding StartYear}" Margin="0,0,0,10"/>

        <TextBlock Text="End Year:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding EndYear}" Margin="0,0,0,10"/>

        <TextBlock Text="Runtime Minutes:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding RuntimeMinutes}" Margin="0,0,0,10"/>

        <TextBlock Text="Genres:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding Genres}" Margin="0,0,0,10"/>

        <TextBlock Text="Rating:" FontWeight="Bold" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding Rating}" Margin="0,0,0,10"/>
    </StackPanel>
</UserControl>
